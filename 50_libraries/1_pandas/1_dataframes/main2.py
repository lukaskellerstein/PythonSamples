import numpy as np
import pandas as pd

# ----------------------------------------------------
# ----------------------------------------------------
# DataFrames
# ----------------------------------------------------
# ----------------------------------------------------
my_df = pd.DataFrame(np.random.randn(3, 4), ["A", "B", "C"], ["W", "X", "Y", "Z"])
print(my_df)


# ----------------------------------------------------
# Create a new column with a sum from two other columns
# ----------------------------------------------------
my_df["NEW"] = my_df["X"] + my_df["Y"]
print(my_df)
